server:
  ssl:
    enabled: false

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/grammr?currentSchema=grammr
    username: user
    password: password

analysis:
  rest:
    host: localhost