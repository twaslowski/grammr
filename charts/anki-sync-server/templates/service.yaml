apiVersion: v1
kind: Service
metadata:
  name: {{ include "anki-sync-server.fullname" . }}
  labels:
    {{- include "anki-sync-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anki-sync-server.selectorLabels" . | nindent 4 }}
