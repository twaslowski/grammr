{{- range $.Values.global.languages }}
{{- if .morphology.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "morphology.fullname" $ }}-{{ .code }}
  labels:
    app: {{ include "morphology.fullname" $ }}-{{ .code }}
    language: {{ .code }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "morphology.fullname" $ }}-{{ .code }}
      language: {{ .code }}
  template:
    metadata:
      labels:
        app: {{ include "morphology.fullname" $ }}-{{ .code }}
        language: {{ .code }}
    spec:
      containers:
        - name: {{ include "morphology.fullname" $ }}-{{ .code }}
          image: "{{ $.Values.image.repository }}:{{ $.Values.global.image.tag }}"
          ports:
            - containerPort: 8000
          env:
            - name: SPACY_MODEL
              value: {{ .model }}
          livenessProbe:
            {{- toYaml $.Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml $.Values.readinessProbe | nindent 12 }}
{{- end }}
{{- end }}