apiVersion: v1
kind: Service
metadata:
  name: {{ include "inflection-ru.fullname" . }}
  labels:
    {{- include "inflection-ru.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "inflection-ru.selectorLabels" . | nindent 4 }}
